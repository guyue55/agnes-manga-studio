# Agnes Studio 全维度审计总报告（第 1–48 轮 · 2026-09-18/19）

> 本文是**索引与结论**；每条发现的完整证据、修复过程、踩坑记录都在 [`issues.md`](./issues.md) 按轮留痕，提交历史逐批可对账（`git log b079fde..HEAD`）。
> 方法论一句话：**一切主张以实测为准**——静态钉（uitest 452）管结构，行为钉（apitest 218 / selftest 135）管契约，真机钉（browser-test 99）管体验，度量报表（ui-audit）管视觉，且每类探针都要先自证"非空跑"。

## 一、最终状态

| 项 | 值 |
|---|---|
| 测试基线 | selftest **135** / apitest **218** / uitest **452** / browser-test **99**（`node tools/run-all.mjs` 全绿，2026-09-18 复跑认证） |
| 度量报表 | `node tools/ui-audit.mjs`：4 视口 × 9 页 **0 发现**（溢出/微字号/对比度 AA/图标名/截断提示） |
| 代码规模 | 47 个收录文件；知识图谱 **196 节点 / 605 边 / 9 层 / 15 步导览**（锚点 `ef8834d`） |
| 本地提交 | 52 个（`b079fde..HEAD`，**未推送**——推送与发版待用户决策） |
| VERSION | **1.0.2 已就位**（server.js 与 package.json 同步，双保险注释在案）——下一版可直接发布，无需再 bump |

## 二、维度地图（十面俱闭合）

| # | 维度 | 工具/证据位 | 结论 |
|---|---|---|---|
| 1 | 功能正确性 | apitest 23 组 + selftest | 高危数据类 4 案（B1/B4/B3/M4…）全修，见 issues §二 |
| 2 | 真实浏览器行为 | browser-test 前段组 | 建项目/交互回归/脏守卫/两击删除全钉 |
| 3 | 前端静态一致性 | uitest 452（含 1.6 棘轮、画风镜像表钉） | 类名/符号/路由调用面零漂移 |
| 4 | UI 布局与可访问性 | ui-audit + issues §六 UX-1~9 | WCAG AA 全过；焦点/键盘轮 16 修复覆盖回退 |
| 5 | 竞品对比 | docs/research/ 三份 + 优化批 | B1 设计语言/B2 体验批/B4 画风分层落地 |
| 6 | 事故级路径 | apitest T4/T5/B6 组 | 降级信封、跨主机 Key 泄露防线、重试落盘、413 契约全行为化 |
| 7 | 安全（XSS/穿越/Origin） | XSS 探针组 + apitest 安全组 | 12 向量×6 页零执行实证；穿越双守卫在位 |
| 8 | 性能容量 | 容量基线组 | 300 镜库首绘 41ms、堆 2MB；按集分页天然封顶 |
| 9 | 全链路 E2E | 轮 9-11/18 六组 | 单发/批量/创作链/脚本/设置/回显全穿；**抓出 video_ready 状态机死态** |
| 10 | 工程链与文档时效 | run-all 门禁、孤儿断言追查、计数横查 | ux-audit 退役丢失断言 2 枚已找回；文档计数全对账 |

## 三、按轮发现总账（一行一条，详情见 issues.md 对应节）

**第 1–2 轮**（§一/§二/§五/§六）：U1 分镜生成静默失败、B1 清空数据谎报、B2 级联勾选谎报、B4 清空本集跨项目误删（4 高危）+ M4-M8/T1/H1；UX-1~9 弹窗键盘、僵尸弹窗、对比度、图标钮零提示、截断无 tooltip、prefers-reduced-motion 等。

**第 3 轮**（§七）：Agnes 2.5 新协议线上事故（按模型代际分流修复）+ V2 批量"秒完全失败"。

**第 4 轮**（§八/§九）：四路独立深审 + 对抗评审——安全 X1-X4、意图违背 R 系列、文档兑现 P 系列、体验 E 系列、工程 T 系列；**D 系列误报驳回 7 条入案**（审计纪律）；后端 B6/B7、09 构建残留清理。

**第 5 轮**（§十）：竞品逆向设计批（配色/字阶/组件语义令牌化）。

**第 6 轮**：T5 收口（206）+ `/understand` 增量（194/578）。

**第 7 轮**：存储型 XSS 12 向量真机零执行；a11y 图标名 0 违例。

**第 8 轮**：容量性能基线入门禁（42ms/43ms/2MB）。

**第 9 轮**：全链路 E2E 首开——**🐛 poller 首片回写不推进 status，`video_ready` 在真实流程永不可达**（词表/徽章/删除回退全在引用死态）；修复 + apitest 行级钉。

**第 10 轮**：批量队列 E2E——E7 断页找回真机证据、取消链 mock 差分零；零新缺陷。

**第 11 轮**：创作链 E2E——拆镜拍平/兜底、补提示词精准补缺、**B4 画风注入云端边界实证**（3/3 水彩短语恰一次）；零新缺陷。

**第 12 轮**：脚本页生成-保存链 + F5 深链复原；修复两枚即时求值竞态断言。

**第 13 轮**：文档陈旧度横查（optimization-plan 唯一陈旧值修正）。

**第 14 轮**：SSE 生命周期（无泄漏）+ 多客户端广播钉。

**第 15 轮**：请求体闸门——**B6"先送 413 再断流"契约首次行为化**（121MB 实证）；日志数组封顶核实。

**第 16 轮**：**覆盖回退案**——ux-audit 退役致弹窗键盘断言失踪若干轮，真按键五钉归位；教训：退役工具必须清点断言去向。

**第 17 轮**：孤儿断言追查第二弹——截断无提示检测找回（自证 trunc=20/noHint=0，非空跑）。

**第 18 轮**：E2E 版图合龙（设置页写入回环 + 成片本地回显），十页面全有真实写链。

**第 19 轮**：本报告落档 + 权威 run-all 定档。

**第 20 轮**：**🐛 灾备链自毁洞（数据丢失级）**——坏 db.json 经 .bak 恢复后，第一次 persist 把坏 main 复制进 .bak 覆盖最后好备份。修复：坏文件先归档 `*.corrupt-<ts>` 留证 + 恢复态双写回 main/bak；selftest 补降级链 7 断言（128→135）。

**第 21 轮**：设置健壮性面复审——NUMERIC_SETTINGS 写入口钳制 + 消费端（poller/jobs/agnes）全兜底，E8 钉早在位，零新发现；models.json 损坏走合法默认（缓存可重取，无需救援，设计决定入案）。

**第 22 轮**：分镜排序自愈钉（乱序插入 2,30,1,10 → 数值序 1,2,10,30）+ SSE 广播钉。（该钉参数名有误，第 46 轮自纠）

**第 23–24 轮**：端口撞车防护三场景**脚本化真机验证**（新增 `tools/port-check.mjs`：同端口同 home 复用 / 同端口异 home 漂移 / 异端口影子实例）；教训：bash 引号拼后台进程曾给出"守卫失效"的**假阳性**，多进程验证必须脚本化。

**第 25–44 轮**：`/understand` 增量更新——锚点 `609559`→`ef8834d`，**47 文件 / 196 节点 / 605 边 / 9 层 / 15 步**；新文件 port-check、audit-summary 入图。三枚管线坑入案：① 裁剪旧图必须命名 `batch-<数字>.json`（技能文档写的 `batch-existing.json` 会被合并脚本**静默丢弃**）② 合并脚本测试路径启发式只认 `*.test.mjs`，**7 条 tested_by 边每次增量必被误删**（故评审阶段不可跳过）③ scan-result 陈旧会漏新增文件。终态校验 0 问题（仅 2 条已声明孤立节点）。

**第 45 轮**：上游错误映射契约补钉（apitest 213→**218**）——错 Key 的 `errorType==='invalid_api_key'` 类型化（UI 靠它区分"Key 无效"与"生成失败"）、失败也留任务记录、设置页连通性测试错 Key 路径。

**第 46 轮**：**M8 承诺行为化**（browser-test 78→**85**）——「断连不得永久锁死按钮」此前只靠 `finally` 加固，**零断言**可捕获"finally 被挪进 try"的回归；补慢失败 mock 组 7 钉（提交态/自动解锁/title 复原/错误可见/可重试/数据未污染）。自纠一条**"为错因通过"**的旧钉（分镜排序钉写了 `episode_number`，handler 只读 `episode`，参数被静默忽略）。AGENTS 新增注意事项：**API 响应不做统一信封**（裸数组/裸对象）。

**第 47 轮**：**测试基建守卫假绿修复**（browser-test 85→**99**）——未捕获异常钩子用一次性 `cdp.eval` 安装，首次 `Page.reload` 后即失效，末尾 `|| []` 兜底使两条"无未捕获异常"断言**长期空跑**；改用 `addScriptToEvaluateOnNewDocument` + 钩子存活自证 + **故意抛错正向对照**。转活后全流程实测 0 异常/0 拒绝。副发现：`#/images` 与 `#/videos` **从未真机挂载**，补 9 页挂载矩阵。

**第 48 轮**：审计**测试自身**——全仓扫描无恒真断言、无自比断言、无零断言分组；非门禁工具复验（ui-audit / port-check）；本报告时效修订（标题/基线/规模/台账补至 48 轮）。

## 四、挂起项（均有理由，非遗漏）

| 项 | 状态 | 理由 |
|---|---|---|
| 1.6 inline 样式长尾迁移 | 缓做 | 棘轮已守增量（rawCount≤44 + 微字号禁增）；纯维护性收益，改动有视觉回归风险 |
| 4.3 角色一致性建模 / 4.7 模型 schema 校验 | 依赖上游 | 需云端能力/真实多模型矩阵支撑 |
| 4.4 | ✗ 不做 | issues §三 驳回理由在案 |
| 老用户自定义模板画风残留 | 已知边界 | 新装已净；改动用户私有数据不可越权 |
| push / 发版 | **等用户** | 58 提交本地待推；VERSION 已 1.0.2 待发布态，发布说明可直接以 audit-summary + issues 充当（仓库并无 CHANGELOG 文件，此前表述系本报告杜撰，已纠） |

## 五、复现与验收命令

```bash
node tools/run-all.mjs        # 四套门禁（≈3 分钟）
node tools/ui-audit.mjs       # 视觉度量报表（按需）
git log --oneline b079fde..HEAD   # 全部留痕提交
```
