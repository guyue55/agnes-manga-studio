# Agnes Studio 全维度审计总报告（第 1–18 轮 · 2026-09-18/19）

> 本文是**索引与结论**；每条发现的完整证据、修复过程、踩坑记录都在 [`issues.md`](./issues.md) 按轮留痕，提交历史逐批可对账（`git log b079fde..HEAD`）。
> 方法论一句话：**一切主张以实测为准**——静态钉（uitest 452）管结构，行为钉（apitest 212 / selftest 128）管契约，真机钉（browser-test 78）管体验，度量报表（ui-audit）管视觉，且每类探针都要先自证"非空跑"。

## 一、最终状态

| 项 | 值 |
|---|---|
| 测试基线 | selftest **128** / apitest **212** / uitest **452** / browser-test **78**（`node tools/run-all.mjs` 全绿） |
| 度量报表 | `node tools/ui-audit.mjs`：4 视口 × 9 页 **0 发现**（溢出/微字号/对比度 AA/图标名/截断提示） |
| 代码规模 | 45 个收录文件；知识图谱 194 节点 / 578 边（锚点 `609559`，其后均为行内改动无结构漂移） |
| 本地提交 | 49 个（`b079fde..HEAD`，**未推送**——推送与发版 bump 待用户决策） |
| VERSION | 1.0.2 未动（发版时随 CHANGELOG 一并 bump） |

## 二、维度地图（十面俱闭合）

| # | 维度 | 工具/证据位 | 结论 |
|---|---|---|---|
| 1 | 功能正确性 | apitest 23 组 + selftest | 高危数据类 4 案（B1/B4/B3/M4…）全修，见 issues §二 |
| 2 | 真实浏览器行为 | browser-test 前段组 | 建项目/交互回归/脏守卫/两击删除全钉 |
| 3 | 前端静态一致性 | uitest 452（含 1.6 棘轮、画风镜像表钉） | 类名/符号/路由调用面零漂移 |
| 4 | UI 布局与可访问性 | ui-audit + issues §六 UX-1~9 | WCAG AA 全过；焦点/键盘轮 16 修复覆盖回退 |
| 5 | 竞品对比 | docs/research/ 三份 + 优化批 | B1 设计语言/B2 体验批/B4 画风分层落地 |
| 6 | 事故级路径 | apitest T4/T5/B6 组 | 降级信封、跨主机 Key 泄露防线、重试落盘、413 契约全行为化 |
| 7 | 安全（XSS/穿越/Origin） | XSS 探针组 + apitest 安全组 | 12 向量×6 页零执行实证；穿越双守卫在位 |
| 8 | 性能容量 | 容量基线组 | 300 镜库首绘 41ms、堆 2MB；按集分页天然封顶 |
| 9 | 全链路 E2E | 轮 9-11/18 六组 | 单发/批量/创作链/脚本/设置/回显全穿；**抓出 video_ready 状态机死态** |
| 10 | 工程链与文档时效 | run-all 门禁、孤儿断言追查、计数横查 | ux-audit 退役丢失断言 2 枚已找回；文档计数全对账 |

## 三、按轮发现总账（一行一条，详情见 issues.md 对应节）

**第 1–2 轮**（§一/§二/§五/§六）：U1 分镜生成静默失败、B1 清空数据谎报、B2 级联勾选谎报、B4 清空本集跨项目误删（4 高危）+ M4-M8/T1/H1；UX-1~9 弹窗键盘、僵尸弹窗、对比度、图标钮零提示、截断无 tooltip、prefers-reduced-motion 等。

**第 3 轮**（§七）：Agnes 2.5 新协议线上事故（按模型代际分流修复）+ V2 批量"秒完全失败"。

**第 4 轮**（§八/§九）：四路独立深审 + 对抗评审——安全 X1-X4、意图违背 R 系列、文档兑现 P 系列、体验 E 系列、工程 T 系列；**D 系列误报驳回 7 条入案**（审计纪律）；后端 B6/B7、09 构建残留清理。

**第 5 轮**（§十）：竞品逆向设计批（配色/字阶/组件语义令牌化）。

**第 6 轮**：T5 收口（206）+ `/understand` 增量（194/578）。

**第 7 轮**：存储型 XSS 12 向量真机零执行；a11y 图标名 0 违例。

**第 8 轮**：容量性能基线入门禁（42ms/43ms/2MB）。

**第 9 轮**：全链路 E2E 首开——**🐛 poller 首片回写不推进 status，`video_ready` 在真实流程永不可达**（词表/徽章/删除回退全在引用死态）；修复 + apitest 行级钉。

**第 10 轮**：批量队列 E2E——E7 断页找回真机证据、取消链 mock 差分零；零新缺陷。

**第 11 轮**：创作链 E2E——拆镜拍平/兜底、补提示词精准补缺、**B4 画风注入云端边界实证**（3/3 水彩短语恰一次）；零新缺陷。

**第 12 轮**：脚本页生成-保存链 + F5 深链复原；修复两枚即时求值竞态断言。

**第 13 轮**：文档陈旧度横查（optimization-plan 唯一陈旧值修正）。

**第 14 轮**：SSE 生命周期（无泄漏）+ 多客户端广播钉。

**第 15 轮**：请求体闸门——**B6"先送 413 再断流"契约首次行为化**（121MB 实证）；日志数组封顶核实。

**第 16 轮**：**覆盖回退案**——ux-audit 退役致弹窗键盘断言失踪若干轮，真按键五钉归位；教训：退役工具必须清点断言去向。

**第 17 轮**：孤儿断言追查第二弹——截断无提示检测找回（自证 trunc=20/noHint=0，非空跑）。

**第 18 轮**：E2E 版图合龙（设置页写入回环 + 成片本地回显），十页面全有真实写链。

## 四、挂起项（均有理由，非遗漏）

| 项 | 状态 | 理由 |
|---|---|---|
| 1.6 inline 样式长尾迁移 | 缓做 | 棘轮已守增量（rawCount≤44 + 微字号禁增）；纯维护性收益，改动有视觉回归风险 |
| 4.3 角色一致性建模 / 4.7 模型 schema 校验 | 依赖上游 | 需云端能力/真实多模型矩阵支撑 |
| 4.4 | ✗ 不做 | issues §三 驳回理由在案 |
| 老用户自定义模板画风残留 | 已知边界 | 新装已净；改动用户私有数据不可越权 |
| push / VERSION bump | **等用户** | 49 提交本地待推；发版随 bump+CHANGELOG |

## 五、复现与验收命令

```bash
node tools/run-all.mjs        # 四套门禁（≈3 分钟）
node tools/ui-audit.mjs       # 视觉度量报表（按需）
git log --oneline b079fde..HEAD   # 全部留痕提交
```
