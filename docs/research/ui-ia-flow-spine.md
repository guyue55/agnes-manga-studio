# 信息架构与流程主线 · 参考 Webeye-Video 的骨架（B5 批次依据）

> 症状（用户原话）："现在的 UI 和创作流程关联不强，整个 项目、功能、UI、能力都很分散"。
> 与 B1–B4 不是同一条轴：那三批治的是**设计语言、体验缺陷、组件质感**，这一批治的是
> **信息架构（IA）与流程主线** —— 东西都在，但用户不知道"现在在哪、下一步点哪儿"。
>
> 参考对象：`/Users/apple/Project/Git/Webeye-Video`（React19+Vite+Tailwind，**只借设计决策、不借代码**）。
> 证据说明：该项目的结论来自一次**只读源码研读**（带 `file:line` 引用），**未逐条复核**；
> 本项目（agnes）侧的结论全部是**自己读代码 + 真机验证**得出的。

## 一、参考项目的骨架（它凭什么"不分散"）

四层递进，每层只回答一个问题：

| 层 | 路由 | 回答的问题 |
| --- | --- | --- |
| 营销页 | `/` | 这东西是什么 |
| 项目列表 | `/workspace` `/projects` | 我有哪些项目 |
| 项目中枢 | `/project/:projectId` | 这个项目做到哪了 |
| 单集工作区 | `/project/:projectId/episode/:episodeId` | 这一集现在该干什么 |

**四层在我们这边的落点**（"项目中枢"那一层我们**没有新加一层路由**，而是把"这个项目做到哪了"
变成**每个页面都能看见**的东西 —— 参考项目只在 `/project/:id` 回答这个问题，用户一进单集工作区就又看不见了）：

| 参考层 | 我们 | 说明 |
| --- | --- | --- |
| 项目列表 | `#/projects` | 项目卡 + "继续创作 · <上次那一段>"（B5.6） |
| 项目中枢 | **常驻流程条 `#pipe-strip` + 侧栏进度徽标**（每个页面都在） | 不新加页面：它回答的问题"这个项目做到哪了"在干活时最需要（B5.4/5.3） |
| 单集工作区 | 各段页面（原著/剧本/分镜/图/视频），集数在页面里切 | 阶段不是路由参数而是**页面**，七段链把它们串起来 |
| 行级"下一步" | 分镜表每行的"下一步：…"（B5.11） | 与三个产出点同源 |

三个可直接搬的设计决策：

1. **导航即进度条**。唯一的全局侧栏把 6 个入口写成"阶段 01–04 + 画布 + 提示词库"，而每个入口的
   **副标题是实时状态徽标**（`N 镜头` / `x/y 视频` / `就绪`）—— 用户不用点进去就知道哪一段还欠着。
2. **阶段不是路由，是持久化字段**。一集 = **一个**工作区，6 个阶段靠 `stage` 字段在同一屏换内容，
   刷新/重开回到你离开的那一段；项目上下文由**路由参数**持有（没有页面自带项目下拉）。
3. **每一行自带"下一步"**。镜头级 4 步 checklist（资产绑定/动作台词/关键帧/视频）+ 一个由同一状态机
   算出的 `下一步：…` CTA。**这不是向导（wizard），是"每行自带下一步"的看板** —— 用户永远不用想
   "我现在该去哪一页"。

## 二、我们的问题（自己读代码数出来的）

**项目上下文有 5 种口径**（本轮实测，`grep` 可复现）：

| 页面 | 原来的写法 |
| --- | --- |
| scripts | `params.project` 存在性检查 → 否则 `state.projects[0]` |
| novel | `params.project_id`（**别名**）→ `state.projects[0]` |
| storyboards / characters / images / videos | `params.project` → `state.projects[0]` |
| assets | `localStorage['agnes.assets.project']` → `''` |

后果不是"代码难看"，而是**用户可见的错**：在 A 页选好项目、切到 B 页又变回第一个项目；
同一个链接在不同页面上指向不同的项目；素材库那个页面切了项目连 URL 都不写（刷新就回去）。

**七段链只在工作台看得见**（补 16 做的 `renderPipeline`），其余 10 个页面都看不到"我卡在哪、
下一步点哪儿"—— 而那正是用户最需要它的地方（他已经在干活了，不在总览页）。

## 三、B5 批次（逐条判断，做/不做都写理由）

| # | 项 | 依据 | 决策 |
| --- | --- | --- | --- |
| 5.1 ✔ | **项目上下文唯一来源** | 参考项目"URL 是唯一真相、没有页面自带项目下拉" | **做**：壳层持有 `state.projectId` + 唯一解析入口 `resolveProjectId`/`rememberProject` + 侧栏那**一个**选择器；规范名 `project`，旧别名 `project_id` 一处迁移 |
| 5.2 ✔ | 删掉 6 个页面级项目选择器，页头只留只读的"我在哪个项目" | 同 5.1（两份控件一份真相） | **已做**：`projectPicker` → `projectLabel`（渲染成 `span.proj-tag`），换项目统一走侧栏；素材库那个下拉是**跨项目筛选器**（另一件事）**保留** |
| 5.3 ✔ | **导航即进度**：侧栏入口带状态徽标 | 参考 `Sidebar.tsx:92-110` | **已做**：徽标数取自 `/api/story/pipeline`（**同一份**数据）；取该入口**最靠前那个没做完的**步（后面的步没做往往正是因为前面没做，显示"待前置"会把可行动的下一步藏起来） |
| 5.4 ✔ | **流程条常驻**：七段链从工作台提到壳层 | 参考"跨阶段常驻的漂移横幅" | **已做**：`public/js/pipeline.js` 一份实现（紧凑条/详细面板/侧栏徽标三处共用），挂在 `#view` **之外**所以页面重挂不会清掉；换页强制刷新（TTL 只挡 SSE 高频重复） |
| 5.5 ✔ | 页头"上游产物 + 下一步" | 参考镜头行 `N/4 完成` + `下一步：…` | **已做**：`head({progress})`，内容由 `progressOf(入口, 项目)` 从**七段链那一份数据**推导（页面只报"我是哪个入口"，不手写上游是谁）；被上游挡住时**不摆"下一步"** —— 否则页头会说"下一步：分集剧本（待前置）"而流程条同时说"下一步：解析出卡片"，两处各说各的 |
| 5.6 ✔ | 项目级"上次停在哪一段" | 参考 `stage` 持久化 + "继续创作 →" | **已做**：`agnes.project.<id>.stage`，项目卡上是"继续创作 · 原著解析"（点整张卡同落点）。**判据只有一个来源**：`rememberStage` 只记**链上**的页，而"在不在链上"取自**服务端** `steps[].nav` 的去重集合（`isChainNav`）—— 手抄一份页面清单＝给同一个事实添第二份口径；**留成"每次现查缓存"也不行**：缓存是**按项目**的，刚换项目时可能还没回来，"这一页在不在链上"会被答成"不在"，于是**第一次访问某个项目永远记不住位置**（偶发、极难查）。**记录时机在页面挂载之后** —— `state.projectId` 是**页面**在挂载时解析出来的，挂载前记会记到上一个项目头上。**与 `next_step` 是两件事**："我上次在哪"（只有本机记得住）≠"这个项目下一步该做什么"（七段链的事实，属于流程条），所以标签写的是"上次在…"，不冒充下一步 |
| 5.7 ✗ | 节点图画布（`@xyflow/react` 泳道图） | 参考 `StageCanvas` | **不做**：依赖视口/连线/命中测试库，零依赖重写是 L+，且漫剧是线性流程 |
| 5.8 ✗ | React/Vite/Tailwind/路由库/`React.lazy` 代码分割 | 参考技术栈 | **不做**：与"零依赖、无构建、改一行即生效"正面冲突；无构建下按需加载 6 个 100KB+ 模块会白屏 |
| 5.9 ✗ | 每个页面记住自己的 UI 位置（`usePersistentState`） | 参考 `StoryBiblePanel.tsx:2073` | **暂不做**：概念可借，但我们已有 `?tab=`/`?sec=` 进 hash（B2.9），够用 |
| 5.10 ✗ | 全局顶栏面包屑 | —— | **不做**：参考项目自己也没有（三个中间页根本不渲染侧栏），侧栏中部那张"项目 + 集数"卡就是它的一致性装置 |
| 5.11 ✔ | **每一行自带"下一步"**（参考的第三条设计决策，此前只做到页级 5.5） | 参考 `ShotWorkbench.tsx:377-385` 镜头级 4 步 checklist + `下一步：…` CTA | **已做**：分镜表每行在三个产出点下面写一行"下一步：补提示词／出分镜图／出视频"。**关键是同源**：三道关（提示词→分镜图→视频）的推导收敛成 `rowSteps(s)` **一份**，产出点与"下一步"都读它 —— 各算一份必然分叉，而分叉的样子正是"点还灰着、文案却说下一步是别的关"（两处各自看都对，B5 教训③）。三条边界：① **第一个没做完的关**才是下一步（与侧栏徽标同一个判据）；② 三道关都齐时**不硬凑**（写"三道关都齐了"，假的下一步比没有更糟）；③ **在跑/失败要说清是哪一关**（写"下一步：出分镜图"会让用户以为该再点一次，其实正在跑）。**正对照**：把"下一步"固定说成"出视频"、`rowNext` 不动 → uitest 全绿（它钉的是推导）、真机那两条红得干净（`txt: 下一步：出视频` vs `i: 0`＝提示词）—— 这正是"钉判据 + 真机验同源"分工的样子 |

## 四、做的时候必须守住的纪律（本项目自己的教训）

- **同一份数据不许算两遍**：侧栏徽标、流程条、页头"下一步"三者都取自 `/api/story/pipeline`
  那一个端点；另算一套的代价不是多几十行，而是**两个数字会分叉**，而分叉没人看得出来。
- **侧栏会被 SSE 高频重渲染**：徽标数据必须**缓存 + 去抖**，否则每个事件一次请求（补 27 的同类）。
- **项目上下文只有一个入口**：任何页面自己解析项目都算回归，uitest 有结构棘轮钉住。
- **快照不是权威**：`state.projects` 是启动那一刻的列表，**不能**拿它判"项目存不存在"。
- **改的是"看不见的骨架"**：这类改动文本断言全绿也说明不了问题（补 35 的教训），
  每一条都要有**真机契约**（切页 / 换项目 / 深链 / 死链四个动作）。
